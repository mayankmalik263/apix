# APIx — SIH26056
# python -m playwright install chromium   <- also needed, after pip install

# collection
httpx>=0.27
playwright>=1.40
scrapling>=0.4
pyyaml>=6.0

# web application
fastapi>=0.110
uvicorn>=0.27
slowapi>=0.1.10          # rate limiting on the public API
itsdangerous>=2.2        # signed session cookies
python-multipart>=0.0.20 # FastAPI Form() on the login route
apscheduler>=3.11        # the daily collection schedule

# tests
pytest>=8.0
